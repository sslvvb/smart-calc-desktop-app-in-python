{
  "name": "python_calc",
  "version": "1.0.0",
  "description": "Python desktop SmartCalc v3.0",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "pack": "electron-builder --dir",
    "dist": "electron-builder",
    "mac": "npx electron-builder build --mac",
    "package": "electron-forge package",
    "digital": "electron-builder --mac"
  },
  "author": "sslvvb",
  "license": "MIT",
  "devDependencies": {
    "electron": "25.0.1",
    "electron-builder": "^24.4.0"
  },
  "build": {
    "appId": "smartcalc-app",
    "mac": {
      "target": "dmg"
    },
    "asar": true
  },
  "dependencies": {
    "axios": "^1.4.0"
  }
}
